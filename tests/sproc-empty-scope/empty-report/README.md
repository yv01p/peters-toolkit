# empty-report/ — provenance (answer key — never sent to a rep)

`EMPTY1-SPROC-XRAY.md` is the migration-plan-side input fixture for the
**empty-backlog** reps (`planning-empty` cell). It is a **real x-ray report**,
generated by running the shipped `sproc-xray` (v0.5.0, no empty-scope
short-circuit yet) over the `empty1` corpus (`../empty1/app`), so it carries
the migration-plan validator markers by construction.

- **Source corpus:** `../empty1` (0 DB-resident routines, 0 logic-bearing
  views, 0 CONSTRAINT_LOGIC, 0 binary DB files).
- **Generated by:** Task 1 Step 2, a fresh blind x-ray rep (sonnet) over a
  README-excluded copy of the corpus. This same run is arm-1's RED evidence
  (the report is BALLOONED — the full five-dimension template rendered for a
  0-routine corpus — see `../baseline-results.md`).
- **Shape:** empty `### Extraction Metrics` table, **no** binary signal
  (Context-Intake "Binary DB files (unparsed) … No | 0"), full validator
  markers (`## Coverage Declaration`, `## 1. Inventory & Completeness`,
  `### Extraction Metrics`, `## Confidence & Coverage Declaration`).
- **Normalization:** none — the rep emitted relative source citations only
  (no sandbox/absolute paths).

The migration-plan empty-backlog rep must accept this at intake (markers
present) and, post-fix, emit an empty backlog + reconciliation + STOP.
